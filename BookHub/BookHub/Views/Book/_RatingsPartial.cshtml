@model IEnumerable<BusinessLayer.Models.RatingDetail>

@if (Model.Any())
{
    foreach (var review in Model)
    {
        <div class="border-bottom mb-2">
            <p>@review.User.Name: @review.Comment</p>
        </div>
    }
}
else
{
    <p>No reviews available.</p>
}